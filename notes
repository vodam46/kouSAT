fix the top level architecture, its a mess
	solver struct
		- parse dimacs file
		- initialize data structures
		- solve

	generalize/modularize parts of the code, to be easily replaceable


specialized data structures
	- clause(s) - for "metadata" (activity/glue level, literals hash (for subsumtion), locked, original problem)
		- dont allocate a new pointer for the clause, put it "next to" the data - flexible array member
	- watcher - clause index, blocked literal


read through the profiler, dont just run the program for no reason


args
	- preprocessing parameters
		- preprocessing rounds
		- variable elimination strictness
		- disable certain steps
	- verbosity (obviously)
	- all solutions
	- disable certain features?


more tests?
	list of directories
	call an advanced solver for the result
	timeouts!!!
	external script
	more test files - cnf generators

	comment in the file?


clear all the todos

more preprocessing
bve
	- https://www.msoos.org/2013/04/a-variable-elimination-improvement/
	- https://www.msoos.org/2014/04/why-its-hard-to-eliminate-variables/

allsat - as an argument, and parameter of the solver?
	- add negation of choices as new clause, backtrack with it

for each function, note the requirements, invariant, and result - O(n) complexity?
	write documentation for the internals

read all the pdfs i downloaded on sat solving
	!!! mark down all the interesting ideas in them

separate solving and data structure init

https://github.com/urbanophile/awesome-sat-solvers

input in dimacs format
output according to SAT competition -> "c TEXT", "v LITERALS", "s (SATISFIABLE, UNSATISFIABLE, UNKNOWN)"
two watched literals
cdcl - try different learning schemes
decision heuristic (ex. vsids)
restarts
model generation -> "v LITERALS 0"

phase saving, rephasing,
clause deletion
clause minimization
(pre|in)processing (pure literal elimination, removal of subsumed clauses, blocked clause elimination, ...)
if (pre|in)processing -> model generation of original formula
proof gen for unsat (DRUP/DRAT) - https://www.msoos.org/2022/04/proof-traces-for-sat-solvers/
assumptions / incremental solving (minisat)
unsat core generation
failed assumption literals
clause vivification

solve statistics
	- preprocessing reductions
	- decisions at conflict
	- backtrack amount
	...

implement everything minisat does

http://www.satlive.org/

random (deterministic?) assignments

when to schedule inprocessing?
	- every restart? (too often?)
	- when luby decreases?
	- when too many clauses?
	- every unit?

internal test files? (keep in the git repo)
	git submodules?

multi threading?
	- search threads -> share clauses (<=certain length, glue, other usability metric?)
	- preprocessor thread?
		- vivification
		- variable elimination
		- probing? (up to a certain depth?)

disconnected components

https://www.msoos.org/2011/01/on-the-new-watchlist-scheme/

find some other blog about sat solving to take ideas from

move *all* allocation to allocate_data

clause vivification

reduce the number of memory alloc's
	- reallocs in clause creation -> not +1, but *1.5?

proper debug information

built in checker for testing

start optimizing - preprocessing takes up suspiciously long
	- nqueens cnf - self subsumtion

NULL checks on memory allocations

preprocessing timeouts

symmetry breaking - https://github.com/markusa4/satsuma

multiple conflict clauses?
	- finish unit propagation completely -> multiple conflicts?

no more malloc((solver->len_variables+1)*sizeof(...)) - allocate only as much as is needed

https://matryoshka-project.github.io

inprocessing
	- keep occurence lists?
	- conflict clause subsumtion -> remove all subsumed

https://www.msoos.org/2010/05/on-failed-literal-probing/

fix printing - in the style of kissat? (but more clear which is which)

memcheck - there might be a memory leak again - preprocessing, occurence lists

read through microsat - https://github.com/marijnheule/microsat

clause deletion - use glues?
	- heuristic for deletion
	- 3 categories for glue deletion - test different numbers/heuristics
		- never delete - 2 glue?
		- keep if useful - 3-6 glue
		- always delete - 7+

some inprocessing with learned clauses
	- subsumtion - dont delete if subsumes original clause
	- self subsuming resolution (shouldnt minimization stop this?)
	- variable elimination?

watchlists - native ternary clauses? - some other clause type?

work on combining preprocessing, probing, and search

split long functions into shorter helper functions
	- unit_propagate, preprocess, minimize, probe, maybe_eliminate, clean_database

read through kitten (then kissat?) source code - some algorithmic improvements?
	- stack struct
	- store clauses on a stack?
	- xor for other literal in unit_propagate

clean up the api

limit probing on large instances - bmc - takes too long

rewrite code to be independent of the way literals are represented?
	- would have to be gradual, itll be hard to find all the little quirks
