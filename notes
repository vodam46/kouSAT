fix the top level architecture, its a mess
	solver struct
		- parse dimacs file
		- initialize data structures
		- solve

	generalize/modularize parts of the code, to be easily replaceable



read through the profiler, dont just run the program for no reason


more tests?
	list of directories
	call an advanced solver for the result
	timeouts!!!


clear all the todos

more preprocessing
bve - sort by occurs(pos) * occurs(neg)
	- https://www.msoos.org/2013/04/a-variable-elimination-improvement/
	- more lenient - not just strictly less clauses
		- figure out some sort of heuristic
	- https://www.msoos.org/2014/04/why-its-hard-to-eliminate-variables/

allsat - as an argument, and parameter of the solver?

for each function, note the requirements, invariant, and result - O(n) complexity?
	write documentation for the internals

read all the pdfs i downloaded on sat solving
	!!! mark down all the interesting ideas in them

separate solving and data structure init

https://github.com/urbanophile/awesome-sat-solvers

input in dimacs format
output according to SAT competition -> "c TEXT", "v LITERALS", "s (SATISFIABLE, UNSATISFIABLE, UNKNOWN)"
two watched literals
cdcl - try different learning schemes
decision heuristic (ex. vsids)
restarts
model generation -> "v LITERALS 0"

phase saving, rephasing,
clause deletion
clause minimization
(pre|in)processing (pure literal elimination, removal of subsumed clauses, blocked clause elimination, ...)
if (pre|in)processing -> model generation of original formula
proof gen for unsat (DRUP/DRAT) - https://www.msoos.org/2022/04/proof-traces-for-sat-solvers/
assumptions / incremental solving (minisat)
unsat core generation
failed assumption literals
clause vivification

solve statistics
	- preprocessing reductions
	- decisions at conflict
	- backtrack amount
	...

implement everything minisat does

http://www.satlive.org/

two watched literals - special watcher struct (store the opposite literal)

random (deterministic?) assignments

when to schedule inprocessing?
	- every restart? (too often?)
	- when luby decreases?
	- when too many clauses?

internal test files? (keep in the git repo)
	git submodules?

multi threading?

disconnected components

https://www.msoos.org/2011/01/on-the-new-watchlist-scheme/

find some other blog about sat solving to take ideas from

move *all* allocation to allocate_data
